import * as React from 'react';
import * as ReactDOM from 'react-dom';

const App = () => {
    return <h1>Hello World from React and TypeScript!</h1>;
};

ReactDOM.render(<App />, document.getElementById('root'));
